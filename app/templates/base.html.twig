<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="utf-8">
  <title>{% block title %}Live foot{% endblock %}</title>
  <meta name="viewport" content="width=device-width, initial-scale=1">

  <style>
    /* Base */
    :root{ --gap:10px; }
    *{ box-sizing:border-box }
    body{
      font-family:system-ui,-apple-system,Segoe UI,Roboto,Helvetica,Arial,sans-serif;
      margin:0; background:#0b0f14; color:#e6edf3;
    }
    header,main{ max-width:1100px; margin:0 auto; padding:16px }
    a{ color:#7aa2f7; text-decoration:none }

    /* Cards / Controls */
    .card{ background:#111827; border:1px solid #1f2937; border-radius:12px; padding:12px; margin:8px 0 }
    label{ font-size:12px; color:#9ca3af }
    select,input[type=date]{ background:#0b1220; color:#e6edf3; border:1px solid #1f2937; border-radius:8px; padding:6px }

    /* Match row */
    .row{
      display:grid;
      grid-template-columns:140px 1fr 60px 1fr;
      gap:8px; align-items:center; padding:8px; border-radius:10px;
    }
    .row:hover{ background:#0f172a }
    .team{ display:flex; gap:8px; align-items:center; min-width:0 }
    .team img{ width:20px; height:20px; border-radius:3px; object-fit:contain; background:#0b0f14 }
    .ellipsis{ overflow:hidden; text-overflow:ellipsis; white-space:nowrap }
    .badge{ font-size:12px; padding:2px 6px; border-radius:999px; border:1px solid #374151 }
    .status.live{ color:#fca5a5; border-color:#fca5a5 }
    .status.scheduled{ color:#a7f3d0; border-color:#a7f3d0 }
    .status.finished{ color:#93c5fd; border-color:#93c5fd }

    /* Simple grid wrapper */
    .grid{ display:grid; gap:12px }

    /* Tabs */
    .tabs a{ margin-right:10px; padding:6px 10px; border-radius:999px; border:1px solid #374151 }
    .tabs a.active{ background:#1f2937 }

    /* Topbar / Logos (optional: used by your logo strip) */
    .topbar{ display:flex; align-items:center; gap:12px; padding:8px 10px }
    .league-strip{ display:flex; gap:10px; align-items:center; overflow-x:auto; scrollbar-width:thin; -webkit-overflow-scrolling:touch; }
    .league-link{ display:inline-flex; align-items:center; justify-content:center; width:32px; height:32px; border-radius:6px; border:1px solid #1f2937; background:#0b0f14 }
    .league-link img{ width:24px; height:24px; object-fit:contain; display:block }
    .league-link:hover{ background:#111827 }

    /* Action buttons row (e.g., Today/Reset/Open league) */
    .actions-row{ display:flex; gap:8px; align-items:center; flex-wrap:wrap }
    .actions-row .btn{ flex:0 0 auto }

    /* Home filters grid (assign class on the container in the home page) */
    .home-filters{
      display:grid;
      grid-template-columns: repeat(5, minmax(120px,1fr));
      gap: var(--gap);
      align-items:end;
    }

    /* Tables */
    .table-wrap{ overflow:auto } /* wrap tables in this for horizontal scroll on small screens */
    .card table{ width:100%; border-collapse:collapse }
    .card th, .card td{ padding:6px }

    /* --------- Responsiveness --------- */

    /* ≤1024px: relax columns, stack some controls */
    @media (max-width: 1024px){
      .home-filters{ grid-template-columns: repeat(3, minmax(140px,1fr)); }
      .row{ grid-template-columns: 110px 1fr 60px 1fr; }
    }

    /* ≤820px: tighten match row */
    @media (max-width: 820px){
      .row{ grid-template-columns: 80px 1fr 58px 1fr; }
    }

    /* ≤640px: two-column filters; actions wrap; table scroll helper */
    @media (max-width: 640px){
      header,main{ padding:12px } /* smaller gutters */
      .home-filters{ grid-template-columns: 1fr 1fr; }
      .actions-row{ justify-content:flex-start }
      /* optional sticky first numeric column in wide tables */
      .card th:nth-child(1), .card td:nth-child(1){ position:sticky; left:0; background:#0b0f14 }
    }

    /* ≤600px: stack match rows cleanly */
    @media (max-width: 600px){
      .row{
        grid-template-columns: 1fr 1fr;
        grid-auto-rows: auto;
        gap: var(--gap);
      }
      .row .when{ grid-column: 1 / 2; }
      .row .score{ grid-column: 2 / 3; justify-self:end; }
      .row .team:first-of-type{ grid-column: 1 / 3; }
      .row .team:last-of-type{ grid-column: 1 / 3; justify-self:stretch; }
      .team img{ width:18px; height:18px }
    }

    /* ≤480px: smaller paddings & headings */
    @media (max-width: 480px){
      .card{ padding:10px }
      .tabs a{ padding:5px 8px }
      h1{ font-size:22px }
      h2{ font-size:18px }
      h3{ font-size:16px }
    }
  </style>

  {% block head %}{% endblock %}
</head>
<body>
<header>
  <h1 style="margin:0">Live Foot</h1>
</header>

<main>
  {% block body %}{% endblock %}
</main>

{% include 'partials/_footer.html.twig' %}
</body>
</html>
